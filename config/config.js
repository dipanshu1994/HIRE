// database url
const databaseURL = 'mongodb://localhost:27017'

// database name
const DBName = 'HireAppyPie'

// mail sender
const mailSender = {
    username: 'dipanshu.sunnyverma@gmail.com',
    password: 'Sunny1994#'
}

// secret of JWT toekn
const jwtSecret = 'secret'

module.exports = {
    databaseURL,
    DBName,
    mailSender,
    jwtSecret
}